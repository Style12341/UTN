#include <iostream>
#include <funciones.h>
using namespace std;

int main()
{
    int indexUsado, counter = 0;
    int elementos[5];
    bool elementosUsados[5] = {0};
    while (counter < 5)
    {
        cout << "Ingrese la posicion" << endl;
    }

    for (int i = 0; i < 5; i++)
    {

        cout << elementos[i] << endl;
    }
    return 0;
}